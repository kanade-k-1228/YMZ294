#include "PlayMusic.h"

// MML char to Freqyency Convert Function
// uint16_t get_freq(byte note) {
//   if (note == Note::None)
//     return 0;
//   return FREQ_TABLE[(byte)note];
// }

// play(int len, int music[2][len]) {
//   for (int i = 0; i < len; i++) {
//     set_freq(music[1][len]);
//   }
// }
